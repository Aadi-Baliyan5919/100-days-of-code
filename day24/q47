#include <stdio.h>

int main() {
    int i, j;
    int n = 5;  // Number of rows

    for(i = 1; i <= n; i++) {       // Loop for each row
        for(j = 1; j <= i; j++) {   // Loop for columns in a row
            printf("*");
        }
        printf("\n");
    }

    return 0;
}
