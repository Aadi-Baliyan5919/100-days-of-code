#include <stdio.h>

int main() {
    int i, j;
    int n = 5;  // Number of rows

    for(i = n; i >= 1; i--) {       // Loop for each row
        for(j = i; j <= n; j++) {   // Loop to print numbers in a row
            printf("%d", j);
        }
        printf("\n");
    }

    return 0;
}
